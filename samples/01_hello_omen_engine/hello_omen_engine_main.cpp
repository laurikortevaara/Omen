#include "Engine.h"


/**
* Sample application for using omen game engine
* This sample just creates an instance of the engine and 
* prints out status information in a console window.
**/
int main(int argc, char *argv[]) 
{
    // Create omen engine instance and get pointer to it
	omen::Engine* engine = omen::Engine::instance();

    // Return immediately
	return 0;
}